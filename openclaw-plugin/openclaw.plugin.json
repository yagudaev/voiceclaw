{
  "id": "openclaw-plugin",
  "name": "VoiceClaw Plugin",
  "version": "0.1.0",
  "description": "VoiceClaw bridge plugin that exposes a VoiceClaw-compatible streaming endpoint backed by OpenClaw agent runs",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "authToken": {
        "type": "string"
      }
    }
  },
  "uiHints": {
    "authToken": {
      "label": "VoiceClaw auth token",
      "help": "Optional shared secret required from the VoiceClaw app via Authorization: Bearer <token>.",
      "placeholder": "optional shared secret",
      "sensitive": true
    }
  }
}
